{
  "name": "schedule-api-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Automated API tests for Schedule application",
  "scripts": {
    "test": "jest --runInBand",
    "test:rooms": "jest --runInBand rooms",
    "test:schedules": "jest --runInBand schedules",
    "test:db": "jest --runInBand db"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
